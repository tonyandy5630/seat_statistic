export const REGION_URL = "/regions";
export const CINEMA_URL = "/cinemas";
export const MOVIE_URL = "/movies";
const COLLECTED_SHOWS = `/collectedshows`;

export const EACH_REGION_URL = `${REGION_URL}/ratio`;

export const REGION_PER_MOVIE_URL = `${REGION_URL}/ratio-per-movie`;

export const MOVIE_PER_CINEMA_URL = `${CINEMA_URL}/ratio-per-movie`;

export const CINEMA_PER_MOVIE_URL = `${MOVIE_URL}/ratio-per-cinema`;

export const MOVIE_PER_REGION_URL = `${MOVIE_URL}/ratio-per-region`;

export const LATEST_RECORD_TIME_URL = `${COLLECTED_SHOWS}/latest-record-time`;
